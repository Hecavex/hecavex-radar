[build-system]
requires = ["setuptools>=80"]
build-backend = "setuptools.build_meta"

[project]
name = "hecavex-radar"
version = "0.1.0"
description = "Python collectors and publisher for the HECAVEX Radar static dashboard."
readme = "README.md"
requires-python = ">=3.12"
license = "Apache-2.0"
dependencies = [
  "tldextract==5.3.2",
  "tzdata==2026.3",
  "websockets==17.0.1",
]

[project.optional-dependencies]
dev = [
  "mypy==2.3.1",
  "pytest==9.1.1",
  "ruff==0.16.4",
]

[project.scripts]
hecavex-sync = "hecavex_radar.sync:run"
hecavex-certstream = "hecavex_radar.collect_certstream:run"

[tool.setuptools.packages.find]
include = ["hecavex_radar*"]

[tool.pytest.ini_options]
testpaths = ["tests_python"]
addopts = "-ra"

[tool.ruff]
target-version = "py312"
line-length = 120

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B", "SIM"]

[tool.mypy]
python_version = "3.12"
strict = true
warn_unreachable = true
pretty = true
